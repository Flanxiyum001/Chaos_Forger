# Build artifacts (anchored: only root-level outputs; do not ignore include/Chaos_Forger/)
build/
cmake-build-*/
/Chaos_Forger
/Chaos_Forger
*.o
*.a
*.d

# Local runtime config (keep config.json as the template)
config.local.json

# Logs / core dumps
*.log
core
core.*
